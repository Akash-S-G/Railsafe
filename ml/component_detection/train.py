"""Phase 3 stub — YOLO11 training with grouped splits."""
# from ultralytics import YOLO
# model = YOLO("yolo11m.pt")
# model.train(data="datasets/rfdd/data.yaml", epochs=100, imgsz=640, batch=16)
# Metrics: mAP50, mAP50:95, Precision, Recall, F1 — see docs/research/evaluation.md
# IMPORTANT: grouped split by sequence_id/video — never random frame split (docs/research/leakage.md)
print("Stub — implement after Phase 2 manifest + data.yaml")
